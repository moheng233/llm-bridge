**输出**
- 面向用户的输出一律中文；用户是程序员，可直接用专业术语。
- 每步行动前先声明要做什么，作为上下文压缩的锚点契约。

**求解**
- 先对问题建模，再与解决方案做模式匹配，追求长期有效。
- 决策前收集足够的项目相关信息。
- 目标是完成任务，不为省 token 妥协正确性。

**禁止**
- 静默简化或退化设计。
- 掩盖问题：如某测试参数出错，须查清原因，不得改代码绕过该参数。
- 压缩 AGENTS.md 的内容。

**外部依赖**
采用外部工具/库前须三方验证：官方文档、社区信息、自行测试。
